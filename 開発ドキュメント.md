# Rewrite Toolkit 開発ドキュメント

## プロジェクト概要
Rewrite Toolkit は、サイト管理者が WordPress のリライトルールやリライトタグをノーコードで管理し、REST API / WP-CLI からも同等の操作を行えるようにするためのプラグインである。URL 設計の反復を開発者に依頼せずに完結させ、公開前にパターン検証を行える運用体制を支援する。

## 必須要件
1. **プラグイン情報**
   - プラグイン名は「Rewrite Toolkit」。
   - テキストドメイン: `rewrite-toolkit`。
   - `/languages` ディレクトリを用意し、`load_plugin_textdomain` を呼び出す。
   - 定数 `REWRITE_TOOLKIT_VERSION`, `REWRITE_TOOLKIT_PATH`, `REWRITE_TOOLKIT_URL` を定義する。

2. **リライト資産の登録**
   - `rewrite_toolkit_rules` オプションに保存されたルールを `add_rewrite_rule()` で登録する。
   - `rewrite_toolkit_tags` オプションに保存されたタグを `add_rewrite_tag()` で登録する。
   - タグで定義された `query_var` がある場合は `query_vars` フィルターで追加する。
   - プラグインの有効化/無効化、設定更新、管理画面の手動操作から `flush_rewrite_rules()` を呼び出せるようにする。

3. **管理画面 UI**
   - 設定メニューに「Rewrite Toolkit」ページを追加する。
   - ページ内で以下のセクションを表示する。
     1. リライトタグ一覧 + 追加フォーム。
     2. リライトルール一覧 + 追加フォーム + 既存ルールのインライン編集フォーム。
     3. 最終フラッシュ日時・フラッシュボタン。
   - `manage_options` 権限でのみ操作できるようにする。
   - `wp_nonce_field()` を使った CSRF 対策を行い、すべての入力は `sanitize_*` 系関数でサニタイズする。
   - ルール/タグの保存・削除時には `settings_errors()` を通してフィードバックを表示する。

4. **REST API**
   - 名前空間 `rewrite-toolkit/v1`。
   - エンドポイント:
     - `GET /rules` : 保存済みルール一覧を返す。
     - `GET /tags` : 保存済みタグ一覧を返す。
     - `POST /flush` : 管理者のみ。ルールをフラッシュしてタイムスタンプを返す。
     - `POST /rules/preview` : `pattern` と `path` を受け取り、PCRE でマッチするか検証結果と `$matches` を返す。管理者のみ。
   - `rest_ensure_response()` でレスポンスをラップする。

5. **WP-CLI コマンド**
   - コマンド名: `wp rewrite-toolkit`。
   - サブコマンド:
     - `list` : ルール一覧を表形式で出力。
     - `flush` : ルールをフラッシュ。
     - `test <path> [--pattern=<regex>]` : ルールまたは任意のパターンで照合し結果を表示。

6. **ヘルパー**
   - ルール/タグのサニタイズ関数を用意する (`rewrite_toolkit_sanitize_rule`, `rewrite_toolkit_sanitize_tag`)。
   - `rewrite_toolkit_format_datetime()` で時刻をローカライズ書式へ整形する。

7. **ドキュメント**
   - プロジェクトルートにこの開発ドキュメントを設置する。
   - プラグインディレクトリに WordPress 形式の `readme.txt` を用意し、概要・使い方・REST API / WP-CLI の説明を記載する。

## 非機能要件
- コードは i18n 対応（`__()`, `esc_html__()` など）を行う。
- WordPress のコードスタイルを基準に可読性を確保する。
- 重大な変更時はリライトルールを自動フラッシュし、同時に「手動フラッシュ」手段も提供する。

## 想定利用フロー
1. 管理者が設定画面で必要なリライトタグとルールを登録する。
2. 保存時にルールが即座に反映され、必要に応じて REST API `POST /rules/preview` で検証を行う。
3. CI や運用スクリプトからは WP-CLI `wp rewrite-toolkit flush` で同期を確実に行う。

## テスト / 確認手順
1. `php -l` で主要 PHP ファイルの構文チェックを行う。
2. WordPress 上でプラグインを有効化し、設定ページにアクセスして UI が表示されること。
3. REST API `GET /wp-json/rewrite-toolkit/v1/rules` で登録済みルールが取得できること。
4. `wp rewrite-toolkit test docs/example` で CLI が動作すること。
