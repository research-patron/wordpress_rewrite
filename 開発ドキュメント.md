
---

# 1. プロジェクト概要

## 1.1 プロジェクト名

りらいとくん

## 1.2 前提・スコープ

- 利用者：開発者本人（個人利用前提）
    
- 対象：WordPress の「投稿（post）」  
    （固定ページやカスタム投稿タイプは対象外／将来拡張）
    
- AI：**Gemini 2.5 シリーズのみ**
    
    - デフォルト：Gemini 2.5 Flash
        
    - 設定で Pro など同シリーズに切り替え可
        
    - 他社 API（OpenAI, Claude 等）は考慮しない
        
- 記事送信：1投稿の本文を**丸ごと Gemini に送る**設計とする
    
    - 想定用途が個人ブログであり、トークン上限にかかりにくい前提
        
    - 非常に長い記事でエラーになった場合は「エラーログに詳細表示」し、手動対応
        
- 対応環境（想定）
    
    - WordPress 6.3+
        
    - PHP 8.0+
        
    - シングルサイト前提（マルチサイトは非サポートでよい）
        

---

# 2. 機能要件

## 2.1 メイン機能一覧

1. 記事分析（LLM フレンドリー度のスコアリング）
    
2. リライト対象記事の自動選別
    
3. LLM フレンドリー構成へのリライト
    
4. QA チェック（品質・安全性）
    
5. 差分表示・ロールバック
    
6. カテゴリー単位の重複コンテンツ検出（簡易版）
    
7. バッチ実行・履歴管理
    
8. エラーログの収集・一覧表示（表形式）
    

### 非対象（今回やらない）

- 多人数利用前提の高度な権限管理
    
- 外部サービスとの連携（Search Console 等）
    
- 高度なベクトル検索による重複検知（埋め込みベース等）
    

---

## 2.2 記事分析（事前分析フェーズ）

### 目的

- 各記事を Gemini に渡し、LLM フレンドリー度を数値評価する。
    
- 評価結果を元に「リライトする価値がある記事」を抽出する。
    

### スコアリング指標（0–5 点）

1. BLUF（冒頭に明確な結論があるか）
    
2. FAQ / 質問形式見出しの有無
    
3. 構造化・スキャンしやすさ
    
4. 独自データ・具体例・事例
    
5. 網羅性・多角的カバレッジ
    
6. 新しさ・更新の必要性
    
7. 専門性・信頼性
    

Gemini による出力内容：

- 上記 7 指標のスコア
    
- 総合スコア（0–5 または 0–100 のどちらかに統一）
    
- 推奨アクション：
    
    - `skip`（リライト不要）
        
    - `light_rewrite`
        
    - `full_rewrite`
        
    - `merge_candidate`（重複・統合候補）
        
- 改善ポイントの簡単な箇条書き
    

これを JSON として受け取り、postmeta に保存する。

### 閾値と判定ロジック（設定で変更可能）

初期値の例：

- 総合スコア ≥ 4.0 → `skip` 扱い
    
- 3.0–3.9 → `light_rewrite`
    
- < 3.0 → `full_rewrite`
    

---

## 2.3 リライト対象記事の自動選別

- 分析結果に基づき、`light_rewrite` と `full_rewrite` の記事を「リライト対象」としてフラグ付け。
    
- 管理画面から
    
    - `full_rewrite のみ`
        
    - `light_rewrite のみ`
        
    - `両方`  
        でフィルタ可能にする。
        

---

## 2.4 リライト（Gemini による再構成）

### 目的

- Ahrefs の推奨構成に沿った「LLM に引用されやすいページ構成」へ自動リライトする。
    

### ターゲット構成

1. タイトル（H1）
    
2. 冒頭 BLUF
    
    - 主な結論（2–3 文）
        
    - 主要ポイントの箇条書き 3–5 個
        
3. 基本概念・定義セクション（H2）
    
4. 主要な質問に答えるセクション群（H2/H3）
    
5. オリジナルデータ・事例・比較
    
6. 多角的な分析・注意点・ベストプラクティス
    
7. FAQ セクション（3–7 問）
    
8. まとめ（ポイント再掲＋次アクション）
    

### リライト時の制約

- 意味・事実関係はできるだけ維持する。
    
- 内部リンク・ショートコード・画像タグなどは壊さない（そのまま残す／再利用する方針をプロンプトで指示）。
    
- 文体（ですます／常体）は設定で指定。
    
- キーワード保護リスト（任意）に含まれる語句は削除／言い換え禁止（プロンプトに渡す）。
    

### 保存方法

- リライト前にリビジョンを作成。
    
- リライト後の本文を `post_content` に反映（モードにより直接公開 or 下書き）。
    
- リライト前リビジョン ID を postmeta に保存。
    

---

## 2.5 QA チェック

### 目的

- リライト結果の品質確認と、重大な問題の検出。
    

### Gemini によるチェック内容

- 元記事と比べて、重要な情報が欠落していないか。
    
- 明らかな事実誤認や矛盾がないか。
    
- 日本語として不自然な箇所・読みづらい箇所以外にないか。
    
- LLM フレンドリー・スコアカードを再評価（前後比較）。
    

### QA 結果による判定

- `OK`：
    
    - ステータスを `qa_passed_updated` に更新。
        
- `NG`（閾値以下または問題あり）：
    
    - ステータスを `qa_failed` に更新。
        
    - QA コメントを postmeta に保存。
        
    - 作業一覧のテーブルに「QA NG」としてハイライト表示。
        

---

## 2.6 重複コンテンツの簡易検出（カテゴリー内）

規模が大きくない前提のため、**シンプルな方法**に絞る。

### 手順

1. 対象カテゴリーの投稿タイトルと要約（Gemini に生成させてもよい）を取得。
    
2. タイトル文字列の類似度（例：類似キーワード数）で粗い候補ペアを生成。
    
3. 各候補ペアの本文を Gemini に渡し、
    
    - 「検索意図と対象読者がどの程度重複しているか（0–100%）」を評価させる。
        
4. 一定以上（例：80%）なら `duplicate_group_id` を同一にする。
    

### 利用

- 「重複コンテンツ」タブで、重複グループと含まれる記事を一覧表示。
    
- まずはフラグ表示のみ（統合処理そのものは後回しでも可）。
    

---

## 2.7 差分表示・ロールバック

- WordPress 標準の `wp_text_diff()` + リビジョン機能を利用。
    
- リライト前リビジョンと現行リビジョンの差分を表示。
    
- 「元に戻す」ボタンで、前リビジョンへ復元（postmeta のステータスも適宜更新）。
    

---

## 2.8 バッチ実行・履歴管理

- カテゴリー単位で「分析バッチ」を作成し、バックグラウンドで順次処理。
    
- バッチには以下を保存：
    
    - 実行日時
        
    - 対象カテゴリー
        
    - 対象記事数
        
    - 分析済み数／リライト済み数／QA 済み数／エラー数
        
- 管理画面「バッチ履歴」で一覧表示。
    

---

## 2.9 エラーログと可視化（重要）

「テストはユーザー自身が行う」前提なので、**エラーを詳細に確認できること**を最重要とする。

### 収集するエラー情報

1. 日時
    
2. 対象投稿 ID・タイトル
    
3. フェーズ
    
    - `analysis` / `rewrite` / `qa` / `duplicate_check`
        
4. バッチ ID（あれば）
    
5. API 呼び出し結果
    
    - HTTP ステータスコード
        
    - エラーコード（Gemini 側コード）
        
    - エラーメッセージ（原文）
        
6. 送信したプロンプト種別
    
    - `analysis_prompt` / `rewrite_prompt` / `qa_prompt` / `dup_prompt` など
        
7. 送信テキストの長さ（文字数）
    
8. 追加情報
    
    - タイムアウトか、レートリミットか、パース失敗か等の内部分類
        

### 表示仕様

- 「エラーログ」タブでテーブル表示。
    
- 各行に上記の主要項目を表示し、クリックで詳細（長いメッセージ）を展開。
    
- エラー発生記事には、作業一覧でも「エラー」ラベルを表示し、エラーログへのリンクを付ける。
    
- エラーは**削除せず蓄積**（個人利用なので容量は問題になりにくい）。
    

---

# 3. 非機能要件（個人利用向けに簡略化）

## 3.1 実行方式

- WordPress の cron（`wp_schedule_event`）または Action Scheduler を利用し、  
    1 度に 1 記事ずつ処理するシンプルなジョブキューとする。
    
- 並列実行は想定しない（個人利用であり、競合リスクが低いため）。
    

## 3.2 冪等性・再実行

- 各フェーズ開始時に、`ai_rw_status` を `processing_*` 状態に更新。
    
- 途中でエラーになった場合は、ステータスを `error_*` にして、  
    同じ記事に新ジョブを投入しない。
    
- 「再実行」ボタンから、手動でその記事／バッチを再キューできる。
    

## 3.3 レート制限・タイムアウト

- Gemini API で 429 / 5xx が返った場合：
    
    - エラーログに記録。
        
    - 同一記事については再実行待ち（手動）。
        
    - 自動再試行は 1 回程度に限定（無限ループ回避）。
        
- タイムアウト（PHP 側／HTTP 側）は同様に、ログに記録後、`error_*` 状態にする。
    

## 3.4 プライバシー

- 個人利用前提のため、  
    特別な PII マスキングは行わない。
    
- ただし、設定で「AI に送信しないカテゴリ」を指定できるようにしておく  
    （たとえば個人的メモ記事など）。
    

---

# 4. システム構成とコンポーネント

## 4.1 主なコンポーネント

1. **設定画面**
    
    - Gemini API キー
        
    - 使用モデル（2.5 Flash / Pro 等）
        
    - スコア閾値
        
    - 文体・キーワード保護
        
    - 除外カテゴリ設定
        
2. **分析・バッチ実行画面**
    
    - カテゴリー選択 → バッチ作成 → ジョブ登録
        
    - バッチ一覧・進捗表示
        
3. **作業一覧画面**
    
    - 投稿ごとのステータス・スコア・最終処理日時
        
    - アクション（差分・ロールバック・再実行）
        
4. **重複コンテンツ画面**
    
    - 重複グループと含まれる投稿
        
5. **エラーログ画面**
    
    - エラー情報のテーブル表示＋詳細モーダル
        
6. **バックグラウンド処理**
    
    - 分析ジョブ
        
    - リライトジョブ
        
    - QA ジョブ
        
    - 重複チェックジョブ
        
7. **Gemini クライアント**
    
    - 分析／リライト／QA／重複判定それぞれのエンドポイント呼び出し・プロンプト組み立てとレスポンス解析。
        

---

# 5. データ設計（簡易版）

## 5.1 postmeta

- `ai_rw_status`
    
    - `unprocessed` / `analyzed_skip` / `analyzed_need_rewrite` /  
        `rewritten_waiting_qa` / `qa_failed` / `qa_passed_updated` / `error_*`
        
- スコア関連：
    
    - `ai_rw_score_bluf`
        
    - `ai_rw_score_faq`
        
    - `ai_rw_score_structure`
        
    - `ai_rw_score_originality`
        
    - `ai_rw_score_coverage`
        
    - `ai_rw_score_freshness`
        
    - `ai_rw_score_expertise`
        
    - `ai_rw_score_total`
        
    - `ai_rw_recommendation`
        
    - `ai_rw_last_analyzed_at`
        
- リライト関連：
    
    - `ai_rw_original_revision_id`
        
    - `ai_rw_last_rewrite_at`
        
    - `ai_rw_last_qa_score`
        
    - `ai_rw_last_qa_comment`
        
- 重複関連：
    
    - `ai_rw_duplicate_group_id`（任意）
        

## 5.2 バッチテーブル（`wp_ai_rw_batches`）

- `id`
    
- `created_at`
    
- `categories`（JSON）
    
- `post_count_total`
    
- `post_count_analyzed`
    
- `post_count_rewrite_target`
    
- `post_count_updated`
    
- `post_count_error`
    

## 5.3 エラーログテーブル（`wp_ai_rw_errors`）

- `id`
    
- `created_at`
    
- `post_id`
    
- `post_title`（キャッシュ）
    
- `batch_id`（NULL 可）
    
- `phase`（analysis / rewrite / qa / duplicate）
    
- `http_status`
    
- `error_code`
    
- `error_message`
    
- `prompt_type`（analysis / rewrite / qa / dup）
    
- `text_length`
    
- `extra_info`（JSON：タイムアウト種別など）
    

※ テーブル作成はプラグイン有効化時に一度だけ行う。  
DB バージョンは `wp_options` に `ai_rw_db_version` として保存。

---

# 6. 代表的フローのまとめ

## 6.1 分析〜リライト〜QA 一括実行フロー

1. 管理画面でカテゴリーを選び、バッチを作成（「分析＋リライト＋QA」モード）。
    
2. バッチ内の各投稿に対し、順次ジョブ実行：
    
    1. 分析 → スコア保存 → `ai_rw_status` 更新。
        
    2. 閾値以下ならリライト → 新本文保存＋リビジョン作成。
        
    3. リライト結果を QA → OK なら `qa_passed_updated`、NG なら `qa_failed`。
        
    4. 各ステップでエラーが出たら、`wp_ai_rw_errors` に記録し、ステータスを `error_*` にする。
        
3. バッチ画面・作業一覧画面・エラーログ画面で結果を確認。
    

---

以上が、Gemini 2.5 シリーズ専用・個人利用前提・エラー可視化重視で再整理した開発ドキュメントです。  
この仕様をベースに、プラグインのディレクトリ構成やクラス設計にすぐ着手できるはずです。