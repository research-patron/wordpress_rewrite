# wordpress_rewrite

This repository contains the **Rewrite Toolkit** WordPress plugin and the development specification (`開発ドキュメント.md`). Install the plugin inside `wp-content/plugins/` to manage custom rewrite tags, register rewrite rules from an admin UI, and use the bundled REST / WP-CLI helpers.
